## 1. 数据模型与迁移

- [x] 1.1 在 SQLite/PostgreSQL 的 `holdings` schema 将 `sort_order` 重命名为 `account_sort_order`，并新增 `discipline_sort_order` 字段
- [x] 1.2 编写双库迁移脚本：完成字段重命名兼容与历史持仓独立排序值回填（稳定且可重复）
- [x] 1.3 校验新建持仓时独立排序默认值分配规则（资产类别内追加到末尾）

## 2. API 排序读写改造

- [x] 2.1 调整纪律表相关持仓查询，改为按 `discipline_sort_order`（次级 `id`）稳定返回
- [x] 2.2 新增或扩展持仓重排接口以支持纪律总览排序写入（含 `assetClass` 维度）
- [x] 2.3 增加服务端校验：仅允许当前用户、同一资产类别内的持仓参与本次排序更新
- [x] 2.4 确认账户页查询使用 `account_sort_order`，避免排序语义串线

## 3. 前端纪律表交互调整

- [x] 3.1 将纪律表排序入口改为小图标按钮，放在“状态”列后并右对齐
- [x] 3.2 调整纪律表排序弹窗调用参数，保存时写入独立排序接口
- [x] 3.3 保留可访问性与可用性（aria-label、tooltip、点击热区），避免图标过小导致不可操作

## 4. 回归验证与文档同步

- [x] 4.1 验证多账户同资产类别在纪律表中的顺序稳定且可持久化
- [x] 4.2 验证账户页排序与纪律表排序互不影响
- [x] 4.3 更新 `project_overview.md` 进展日志与相关说明，记录新排序语义变更
- [x] 4.4 补充实现说明到 change 文档（必要时更新相关 spec 注释与 API 注释）
