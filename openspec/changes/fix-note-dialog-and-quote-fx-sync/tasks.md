## 1. 笔记弹窗骨架与滚动稳定化

- [ ] 1.1 重构 `src/components/discipline-notes-fab.tsx` 的 Dialog 布局，为桌面端阅读/编辑态提供同一套固定高度骨架，并为移动端切换到占满可视区域的全屏式布局
- [ ] 1.2 拆分便签列表滚动区与正文滚动区，让阅读框/编辑框复用同一个正文容器，确保长内容滚动时只有正文区域内部移动
- [ ] 1.3 回归验证桌面端模式切换不跳动、移动端不裁切关键操作，且正文滚动不会带动整个弹窗主体

## 2. 笔记失焦自动保存

- [ ] 2.1 移除笔记显式“保存”按钮，并为当前笔记维护草稿与已保存快照的 dirty-check 状态
- [ ] 2.2 在退出编辑态、切换笔记、关闭弹窗或离开当前笔记编辑上下文时触发自动保存；保存失败时保留草稿并提示错误
- [ ] 2.3 补充笔记交互回归，覆盖“修改后失焦自动保存”“切换笔记前保存当前修改”“自动保存失败不丢内容”

## 3. 报价同步联动汇率刷新

- [ ] 3.1 在 `src/lib/services/holding-price-sync-service.ts` 中显式接入汇率刷新检查，确保 `manual`、`silent-client`、`cron` 三种触发来源复用同一逻辑
- [ ] 3.2 调整 `POST /api/holdings/fetch-prices` 相关链路，使“无任何 updated 项”场景仍会执行汇率刷新检查，同时保持现有 `updated/failed/skipped` 返回结构兼容
- [ ] 3.3 为汇率联动补充回归验证，覆盖“过期汇率刷新成功”“同日重复请求命中缓存”“汇率外部源失败时回退缓存或默认值且不阻断报价结果”

## 4. 验收、文档与提交

- [ ] 4.1 手工验收桌面端与移动端笔记弹窗：尺寸稳定、正文内部滚动、失焦自动保存、生效后重新打开内容一致
- [ ] 4.2 完成实现后同步更新相关主 spec 与 `project_overview.md`，记录笔记弹窗交互修复和报价联动汇率刷新变更
- [ ] 4.3 仅提交本次任务相关文件，并按项目规范创建 commit
