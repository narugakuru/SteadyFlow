## 1. 数据模型与接口

- [x] 1.1 为 `accounts` 增加默认排序字段，补充 SQLite / PostgreSQL 迁移与历史顺序回填，并更新账户类型定义
- [x] 1.2 调整账户查询与创建逻辑以读写默认排序字段，并新增账户重排接口与服务端校验

## 2. 账户页排序交互

- [x] 2.1 新增账户排序弹窗，复用现有拖拽基础设施实现账户默认顺序的拖拽保存
- [x] 2.2 更新账户列表组件，接入排序按钮与“总价值 / 持仓盈亏 / 现金 / 持仓数”表头三态排序，并统一桌面与移动端展示结果

## 3. 验证与文档同步

- [x] 3.1 执行相关验证，确认账户默认排序、新建账户追加末尾、表头三态排序与刷新保持行为正常
- [x] 3.2 将功能变更同步到 `openspec/specs`、`openspec/project.md` 与 `project_overview.md`
