## 1. 笔记弹窗布局调整

- [x] 1.1 调整 `discipline-notes-fab` 的桌面端主编辑区层级，移除冗余说明标签并将经典句子下沉到底部
- [x] 1.2 调整移动端弹窗布局顺序，将当前笔记编辑区置顶、便签列表移动到底部并保持独立滚动
- [x] 1.3 将删除按钮改为与账户页一致的红色垃圾桶图标按钮

## 2. Quote 兼容与数据流

- [x] 2.1 将经典句子展示改为前端本地数组随机抽取，不再读取已保存笔记的 quote 作为展示来源
- [x] 2.2 放宽纪律笔记创建/更新接口对 `quote` 的校验，保留数据库兼容写入但不要求前端依赖该字段

## 3. 验证与文档同步

- [x] 3.1 运行 `eslint` 与 `typecheck`，复核桌面端与移动端笔记弹窗布局改动的代码路径
- [x] 3.2 同步 `openspec/specs/discipline-notes/spec.md` 与 `project_overview.md`，并完成本阶段提交
