## 1. 缓存基础设施

- [x] 1.1 引入客户端查询缓存与 IndexedDB 持久化能力，并在应用根布局接入 Provider
- [x] 1.2 新增统一缓存策略配置文件（如 `src/lib/cache/policy.ts`），固化 `staleTime=60s` 与 `persistTime=3d`
- [x] 1.3 定义统一 query key 规范与 `userId` 分区规则（含版本前缀）
- [x] 1.4 封装通用查询 hook，使页面层可按策略读取“缓存优先 + 条件刷新”数据

## 2. 全局行为与一致性

- [x] 2.1 实现缓存年龄判定逻辑：`<=60s` 不异步刷新，`>60s 且 <3d` 异步刷新，`>=3d` 视为持久化失效
- [x] 2.2 实现全局后台刷新失败通知条（缓慢自动淡出，不阻塞交互）
- [x] 2.3 实现低侵入数据新鲜度展示组件（小字号、低视觉权重）
- [x] 2.4 实现登出与 401 场景的当前用户缓存清理
- [x] 2.5 实现写操作失效映射机制与跨标签页失效同步（BroadcastChannel）

## 3. 页面迁移

- [x] 3.1 迁移 Dashboard 到统一缓存读取策略，替换全页阻塞加载为缓存优先展示
- [x] 3.2 迁移 Accounts 页面及其依赖读取（账户/资产配置）到统一缓存策略
- [x] 3.3 迁移 Transactions 页面读取（交易/账户/持仓）到统一缓存策略
- [x] 3.4 迁移 Batch Update 页面读取（账户/持仓）到统一缓存策略
- [x] 3.5 迁移 Netvalue、Market、Admin 页面的读取到统一缓存策略
- [x] 3.6 移除页面内分散的 `setLoading(true)+直接 fetch` 阻塞模式

## 4. 验证与文档

- [ ] 4.1 补充关键场景验证：F5 首屏命中、60 秒内不刷新、60 秒后后台刷新、失败通知条、401 清理
- [ ] 4.2 验证多标签页一致性与多账号切换隔离
- [x] 4.3 更新 `project_overview.md` 进展日志与技术栈/目录结构（如新增缓存层依赖与目录）
- [ ] 4.4 通过 lint/typecheck 与手工回归后进入 `/opsx:apply` 执行实现
