## 1. 导出接口与快照聚合

- [x] 1.1 新增投资组合导出聚合逻辑，组装 `meta`、`summary`、`raw`、`derived` 四层 JSON 快照
- [x] 1.2 新增 `GET /api/export/portfolio` 路由，并实现未登录保护与当前用户数据隔离
- [x] 1.3 为导出接口增加 `download=1` 下载响应与稳定文件命名
- [x] 1.4 对导出中的 settings 做白名单过滤，确保不返回报价供应商 key 与认证敏感信息

## 2. 报价同步元数据

- [x] 2.1 定义并封装 `quote_sync.*` 元数据键的读写与标准化读取逻辑
- [x] 2.2 在手动、静默、Cron 三种报价同步触发路径中统一写入开始/完成/成功时间、状态、来源与摘要
- [x] 2.3 将标准化 `quoteSync` 元数据接入导出接口响应，供 `meta.quoteSync` 输出复用

## 3. 报价刷新链路扩展

- [x] 3.1 扩展 `POST /api/holdings/fetch-prices` 支持 `manual`、`silent-client`、`cron` 触发来源语义
- [x] 3.2 保持手动模式返回逐条结果明细结构不变，并兼容静默模式复用同一结果结构
- [x] 3.3 扩展现有 `POST /api/cron/netvalue` 链路，在每日报价同步时记录统一的 quote sync 元数据

## 4. Dashboard 交互与静默兜底

- [x] 4.1 在首页“更新股价”按钮旁新增“导出”按钮，并接入 JSON 快照下载
- [x] 4.2 在总资产卡片中新增弱化的“股价上次更新时间”提示，并与前端缓存时间区分展示
- [x] 4.3 在 Dashboard 加入静默兜底刷新判断：仅在股价超过阈值且无进行中同步时自动触发
- [x] 4.4 确保静默刷新不弹出股价更新结果弹窗，但完成后会刷新页面资产数据与时间提示

## 5. 验证与文档同步

- [x] 5.1 验证导出接口在浏览器或 API 客户端中可直接查看与下载，且只返回当前用户数据
- [x] 5.2 验证手动刷新、静默刷新、每日 Cron 三条路径的 quote sync 元数据口径一致
- [x] 5.3 更新 `project_overview.md`、相关主 specs 与必要说明文档，记录导出接口与静默报价刷新变更
