## 1. 净值服务与时区基础

- [x] 1.1 抽取 `src/lib/netvalue-service.ts`，把当日净值计算与写入逻辑从 `/api/netvalue` 路由下沉为可复用服务函数。
- [x] 1.2 在净值服务中实现基于用户时区的业务日期计算（默认 `Asia/Shanghai`），并统一替换现有 `toISOString().slice(0,10)` 日期来源。
- [x] 1.3 将净值写入改为 `(userId, date)` 原子 upsert，保证同日多触发幂等覆盖。

## 2. 统一自动触发封装

- [x] 2.1 新增 mutation 包装器（如 `runMutationWithNetvalue`），统一执行“主业务成功后触发当日净值刷新”。
- [x] 2.2 在账户 API（创建/编辑/删除）接入统一包装器。
- [x] 2.3 在持仓 API（创建/编辑/删除、批量更新、自动获取报价）接入统一包装器。
- [x] 2.4 在交易 API（创建/删除）接入统一包装器。

## 3. Dashboard 与手动入口收敛

- [x] 3.1 移除 Dashboard 顶部“记录净值”按钮与对应事件处理逻辑。
- [x] 3.2 保留并验证“自动获取报价”交互不受影响，且净值刷新由自动触发链路承担。

## 4. 用户时区设置

- [x] 4.1 复用 `settings` 增加净值时区配置键（如 `netvalue.timezone`），实现读取、写入与默认值回退。
- [x] 4.2 增加时区值校验（IANA 时区），非法值拒绝写入并在读取时回退 `Asia/Shanghai`。
- [x] 4.3 在现有设置入口增加“净值时区”可编辑项，支持用户手动调整。

## 5. 每日 03:00 自动记录

- [x] 5.1 新增受密钥保护的 cron 路由（如 `POST /api/cron/netvalue`），按用户时区判断是否命中本地 03:00 触发窗口。
- [x] 5.2 在 cron 执行中调用统一净值服务，为命中窗口的用户创建/刷新当日净值，并输出执行统计日志。
- [x] 5.3 增加调度配置（云端 cron + 本地离线可用调度说明），确保每日自动记录能力可部署。

## 6. 回归验证与文档

- [x] 6.1 回归验证：账户/持仓/交易/批量改价成功后均会自动刷新当日净值，同日无重复记录。
- [x] 6.2 回归验证：不同时区用户的业务日期正确，且在各自本地凌晨 3:00 可被定时任务命中。
- [x] 6.3 更新相关文档（OpenSpec、README/运维说明）描述净值自动化触发、时区设置与定时任务配置。
