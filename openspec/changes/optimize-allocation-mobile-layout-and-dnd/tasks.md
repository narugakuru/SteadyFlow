## 1. 纪律明细数据规则

- [x] 1.1 在资产配置纪律数据构建链路中新增金额为 0 标的过滤逻辑，并确认不影响类别汇总金额与占比计算
- [x] 1.2 为兼容路径补充“0 金额标的自动排序到末位（次级按既有排序字段）”规则，保证非 0 金额项顺序稳定
- [x] 1.3 为纪律展开接口/聚合函数补充单元测试或集成测试，覆盖“0 金额隐藏”和“0 金额末位排序”场景

## 2. 移动端资产类别卡片重排

- [x] 2.1 重构移动端纪律卡片为三行堆叠布局：标题+进度、核心金额、盈亏+状态+调仓按钮
- [x] 2.2 调整移动端金额与状态样式，验证 6 位及以上金额不溢出、不遮挡、不压缩关键操作
- [x] 2.3 保持桌面端纪律表布局不变，并补充断点分支回归检查

## 3. 拖拽排序回弹修复

- [x] 3.1 调整移动端拖拽结束逻辑：drop 后先本地落地新顺序，再异步提交持久化请求
- [x] 3.2 持久化成功保持当前顺序，失败时回滚到上一次确认顺序并给出错误提示
- [x] 3.3 为拖拽排序链路补充回归测试，覆盖“释放即稳定”“刷新后顺序保持”“请求失败回滚”

## 4. 验收与文档同步

- [ ] 4.1 在真实移动端视口完成手工验收：0 金额项显示规则、卡片三行布局、拖拽不回弹
- [x] 4.2 更新 `openspec/specs` 对应主 spec（同步 delta 结果）与 `project_overview.md` 进展日志
- [x] 4.3 完成本 change 所有改动后按项目规范提交 commit（仅提交本次任务相关文件）
