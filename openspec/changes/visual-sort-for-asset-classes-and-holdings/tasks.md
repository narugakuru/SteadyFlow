## 1. 数据模型与迁移

- [x] 1.1 为 `holdings` 增加排序字段（如 `sort_order`），同步更新 SQLite/PostgreSQL schema
- [x] 1.2 编写 SQLite/PostgreSQL 迁移脚本并回填历史持仓排序（确保旧数据可用）
- [x] 1.3 校验并更新 Drizzle meta（journal/snapshot），确保双数据库迁移链可通过 check

## 2. API 排序读写能力

- [x] 2.1 资产类别 API 支持批量更新 `sortOrder` 并保持目标占比校验逻辑兼容
- [x] 2.2 持仓 API 增加排序更新能力（批量或专用 reorder 接口），并校验用户数据隔离
- [x] 2.3 统一资产类别与持仓查询接口按 `sortOrder`（次级 id）返回稳定顺序

## 3. 前端可视化排序交互

- [x] 3.1 在资产配置设置页实现资产类别上移/下移交互，并保存排序结果
- [x] 3.2 在账户页新增“排序”按钮并实现持仓排序弹窗（仅展示名称/股票编号/账户归属 + 右侧拖拽句柄）
- [x] 3.3 在纪律表展开持仓区复用同一排序结果，确保与账户页顺序一致
- [x] 3.4 实现“拖拽仅本地生效、点击保存后一次性批量写库、取消不落库”的交互与接口调用
- [x] 3.5 新增资产类别/持仓后默认追加末尾，并在 UI 中即时反映

## 4. 交易页面表格改造

- [x] 4.1 将交易页面列表改为横向表格，并按固定顺序展示列：账户、标的名称、操作类型、股数、股价、金额、手续费、日期
- [x] 4.2 将交易删除按钮改为与账户页一致的小垃圾桶图标按钮样式
- [x] 4.3 完成交易页面桌面/移动端验收，确保横向表格滚动、字段可读性与删除操作可用

## 5. 兼容性与验收

- [x] 5.1 验证历史数据升级后排序可用（含“股票基金”兼容到股票优先级）
- [x] 5.2 验证多终端/刷新后顺序持久化与跨页面一致性
- [x] 5.3 验证持仓拖拽弹窗在桌面/移动端可用（拖动手势、保存、取消）
- [x] 5.4 补充或更新相关文档与变更日志（project_overview、OpenSpec 任务状态）
