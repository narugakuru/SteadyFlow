## 1. 数据模型与迁移

- [ ] 1.1 为 `holdings` 增加排序字段（如 `sort_order`），同步更新 SQLite/PostgreSQL schema
- [ ] 1.2 编写 SQLite/PostgreSQL 迁移脚本并回填历史持仓排序（确保旧数据可用）
- [ ] 1.3 校验并更新 Drizzle meta（journal/snapshot），确保双数据库迁移链可通过 check

## 2. API 排序读写能力

- [ ] 2.1 资产类别 API 支持批量更新 `sortOrder` 并保持目标占比校验逻辑兼容
- [ ] 2.2 持仓 API 增加排序更新能力（批量或专用 reorder 接口），并校验用户数据隔离
- [ ] 2.3 统一资产类别与持仓查询接口按 `sortOrder`（次级 id）返回稳定顺序

## 3. 前端可视化排序交互

- [ ] 3.1 在资产配置设置页实现资产类别上移/下移交互，并保存排序结果
- [ ] 3.2 在账户页持仓列表实现标的上移/下移交互，并保存排序结果
- [ ] 3.3 在纪律表展开持仓区复用同一排序结果，确保与账户页顺序一致
- [ ] 3.4 新增资产类别/持仓后默认追加末尾，并在 UI 中即时反映

## 4. 兼容性与验收

- [ ] 4.1 验证历史数据升级后排序可用（含“股票基金”兼容到股票优先级）
- [ ] 4.2 验证多终端/刷新后顺序持久化与跨页面一致性
- [ ] 4.3 补充或更新相关文档与变更日志（project_overview、OpenSpec 任务状态）
