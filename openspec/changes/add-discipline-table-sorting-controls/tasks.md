## 1. OpenSpec 与排序状态建模

- [ ] 1.1 补齐 change proposal/design/specs/tasks，明确纪律表类别内三态排序、本地持久化与移动端 helper 行要求
- [ ] 1.2 新增纪律表本地排序状态辅助逻辑，封装字段/方向校验与 localStorage 读写

## 2. 纪律表排序交互实现

- [ ] 2.1 更新 `discipline-table`，为桌面端金额/持仓盈亏表头接入三态排序交互与激活样式
- [ ] 2.2 更新 `discipline-table`，让每个资产类别的展开明细按本地排序偏好临时重排，默认回退到数据库手动顺序
- [ ] 2.3 为移动端纪律表新增 sticky 排序辅助栏，并与桌面端共享同一排序状态反馈

## 3. 验证与文档同步

- [ ] 3.1 执行相关验证，确认类别顺序保持、三态循环正确、刷新后本地排序偏好生效
- [ ] 3.2 将功能变更同步到 `openspec/specs` 与 `project_overview.md`，然后提交本阶段变更
