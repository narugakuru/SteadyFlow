## 1. Cron 编排改造（先价后值）

- [x] 1.1 抽取可复用的“按用户执行股价更新”服务函数，避免 cron 通过 HTTP 内调接口
- [x] 1.2 在 `POST /api/cron/netvalue` 中实现每用户“先股价更新、后净值写入”的顺序编排
- [x] 1.3 将每用户股价更新统计（updated/failed/skipped）汇总到 cron 响应结构

## 2. 宽松模式与状态标记

- [x] 2.1 实现宽松模式：无论报价结果 `ok/partial/failed`，都执行当日净值 upsert
- [x] 2.2 为每用户结果增加 `quoteSyncStatus`（ok/partial/failed）与失败原因摘要
- [x] 2.3 验证“全部报价失败”场景下仍可成功写入当日净值

## 3. 分批执行与游标续跑

- [x] 3.1 增加按用户分批处理能力（可配置批次大小）
- [x] 3.2 增加剩余时间预算检测，接近超时阈值时提前停止新用户处理
- [x] 3.3 保存并读取处理游标（上次处理位置），实现下次 cron 续跑
- [x] 3.4 验证游标到末尾后重置到起点，形成循环覆盖

## 4. 验证与文档

- [x] 4.1 运行 typecheck 与相关 lint，确保 cron 改造与新增结构类型正确
- [x] 4.2 补充 README/运维文档，说明每日任务宽松模式、分批策略与补偿方式
- [x] 4.3 补充 project_overview 进展日志，记录“先价后值 + 分批续跑”能力变更
